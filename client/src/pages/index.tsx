import React, { FC } from 'react';
import Head from 'next/head';

const Home: FC = () => (
  <div>
    <Head>
      <title>Monsters RPG</title>
    </Head>

    <main />
  </div>
);

export default Home;
